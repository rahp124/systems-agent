[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "water-investigation-spike"
version = "0.1.0"
description = "Throwaway correctness and simulator feasibility spike for water-network investigation."
requires-python = ">=3.12"
dependencies = [
  "epyt-flow==0.17.2",
  "numpy>=2.0",
]

[project.optional-dependencies]
dev = ["pytest>=9.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.setuptools]
packages = ["water_investigation"]
